% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_switch.R
\name{as_va_switch_data}
\alias{as_va_switch_data}
\title{Build a visit-aligned analysis object for the switching estimands}
\usage{
as_va_switch_data(subject_data, visit_L, visit_times, tau = max(visit_times))
}
\arguments{
\item{subject_data}{Data frame with one row per subject and columns \code{id},
\code{A0} (randomized arm, 0/1), \code{W1}, \code{W2} (baseline covariates), \code{T_time}
(failure time), \code{R_time} (treatment-switch time; \code{Inf} if never),
\code{C_time} (censoring/dropout time; \code{Inf} if never). Times are on the same
scale as \code{visit_times} and \code{tau}; administrative censoring beyond \code{tau}
should be coded as \code{C_time = Inf}.}

\item{visit_L}{Numeric matrix, one row per subject (row order matching
\code{subject_data}) and one column per entry of \code{visit_times}, giving the
visit covariate \code{L} measured at each scheduled visit. Values after a
subject leaves observation may be \code{NA}.}

\item{visit_times}{Numeric vector of scheduled visit times, starting at 0.}

\item{tau}{Analysis horizon.}
}
\value{
A \code{va_switch_data} object.
}
\description{
Converts a one-row-per-subject table of exact event times plus a
subject-by-visit matrix of the time-varying covariate into the object
consumed by \code{\link[=va_ct_switch]{va_ct_switch()}}.
}
\details{
The current version uses two baseline covariates (\code{W1}, \code{W2}) and a single
scheduled-visit covariate \code{L} (measured at each \code{visit_times}), matching the
estimator in McCoy (2026). Map your trial's covariates onto these columns.
}
