% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_switch.R
\name{va_ct_switch}
\alias{va_ct_switch}
\title{Estimate ITT or per-protocol failure-free survival with VA-CT-TMLE}
\usage{
va_ct_switch(
  obs_data,
  estimand = c("ITT", "PP"),
  tau = obs_data$tau,
  g0 = 0.5,
  weight_trunc = NULL,
  M = 100,
  B = 10,
  max_iter = 10,
  seed = 1L
)
}
\arguments{
\item{obs_data}{A \code{va_switch_data} object from \code{\link[=as_va_switch_data]{as_va_switch_data()}} or
\code{\link[=simulate_va_switch_trial]{simulate_va_switch_trial()}}.}

\item{estimand}{\code{"ITT"} (treatment-policy: natural switching, censoring
removed) or \code{"PP"} (hypothetical no-switching: switching and censoring
removed).}

\item{tau}{Analysis horizon.}

\item{g0}{Known randomization probability of arm 1.}

\item{weight_trunc}{Optional cumulative-weight control under positivity
stress: a numeric absolute cap, or a string like \code{"p95"} for the 95th
percentile of the positive weights (recommended when switching is heavy).
\code{NULL} (default) applies no truncation.}

\item{M}{Monte Carlo draws for visit-level integration.}

\item{B}{Sub-bins for within-interval hazard targeting.}

\item{max_iter}{Maximum targeting iterations.}

\item{seed}{Random seed.}
}
\value{
A \code{vacttmle} object; the reported risk difference is
risk under arm 1 minus risk under arm 0 (failure by \code{tau}).
}
\description{
Estimate ITT or per-protocol failure-free survival with VA-CT-TMLE
}
